<!DOCTYPE html>
<html>
  <head>
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>OriginalApp38253</title>
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>
    <link rel="stylesheet" type="text/css" href="https://yui.yahooapis.com/3.18.1/build/cssreset/cssreset-min.css">
    <%= stylesheet_link_tag 'application', media: 'all', 'data-turbolinks-track': 'reload' %>
    <%= javascript_pack_tag 'application', 'data-turbolinks-track': 'reload' %>
    <script src="https://ajax.googleapis.com/ajax/libs/jquery/2.1.0/jquery.min.js"></script>
  </head>

  <body>
    <header>
      <div class="header-left">
        <%= link_to image_tag("header-title.png"), root_path %>
        <div class="person">
          <div class="heart-button"><%= link_to image_tag("header-a.png") %></div>
          <div class="heart-button-blue"><%= link_to image_tag("header-b.png") %></div>
          <div class="heart-button-yellow"><%= link_to image_tag("header-c.png") %></div>
        </div>
      </div>
      <div class="header-right">
        <% if user_signed_in? %>
          <%= link_to "#{current_user.group_name}さん", edit_user_registration_path %>
          <%= link_to 'ログアウト', destroy_user_session_path, method: :delete %>
        <% else %>
          <%= link_to "新規登録", new_user_registration_path %>
          <%= link_to "Log in", new_user_session_path %>
        <% end %>
      </div>
    </header>
    <%= yield %>
  </body>
</html>
